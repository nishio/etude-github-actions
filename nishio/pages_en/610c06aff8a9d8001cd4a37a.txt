Scrapbox.Project.pagesMystery of much more pages than actual number of pages
from [Scrapbox look-back function].

`scrapbox.Project.pages.length === 21207`
What's with the page count that's much higher than the actual number of pages?

Are there a lot of `updated === 0`?
