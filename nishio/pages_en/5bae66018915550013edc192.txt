Crawl Scrapbox once a day
	I could program it to throw messages to Slack.
 I could use cron to crawl the Scrapbox once a day, do some processing, and then throw it to Slack.
 Implement the crawl portion of Scrapbox

[Machine reads Scrapbox].

[Scrapbox corpus]
